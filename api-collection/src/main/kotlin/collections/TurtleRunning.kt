package collections

fun main(){

    while (true) {

        try {

            val l = readLine()!!.toInt()
            val v = readLine()!!.split("        ").run {     {      () } }
            v.sort()
            when (v.last()) {

            }

        } catch (f :  ) {
            break
        } catch (n : ) {
            break
        } catch (e :) {
            break
        }
    }
}